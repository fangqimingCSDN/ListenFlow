# ============================================================
# 实时语音转写系统 - 后端依赖
# ============================================================

# Web 框架
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
websockets==12.0

# 异步支持
aiofiles==23.2.1
aiohttp==3.9.5

# ── ASR 后端 ─────────────────────────────────────────────────
# FunASR: 中文 SOTA，paraformer-zh，非自回归极速推理（默认后端）
funasr==1.1.6
modelscope==1.17.1
# modelscope/funasr 必要依赖
transformers>=4.40.0
accelerate>=0.30.0
sentencepiece>=0.2.0
protobuf>=4.25.0
onnxruntime>=1.18.0

# faster-whisper: 多语言，设置 ASR_BACKEND=whisper 时使用
# 比原版 Whisper 快 4x（CTranslate2），GPU/CPU 均支持
faster-whisper==1.0.3
ctranslate2>=4.0.0

# Qwen3-ASR: LLM-based ASR, set ASR_BACKEND=qwen3 to use
# Chinese CER beats paraformer, supports system prompt, native unlimited-length audio
# Requires transformers>=4.45.0 (Qwen2Audio support) - already declared above

# ── VAD ─────────────────────────────────────────────────────
torch>=2.1.0
torchaudio>=2.1.0
silero-vad==5.1

# ── 说话人识别 ───────────────────────────────────────────────
# ERes2Net 声纹提取已含在 modelscope 中
# OnlineSpeakerCluster 为项目内实现（无额外依赖）

# ── 音频处理 ─────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.13.0
soundfile==0.12.1
librosa==0.10.2
pydub==0.25.1

# ── 数据库 ───────────────────────────────────────────────────
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9

# ── 向量数据库（ENABLE_VECTOR_STORE=true 时需要）────────────
# Milvus：推荐生产级，支持十亿级向量，分布式，HNSW 索引
pymilvus==2.4.3
# 文本向量化：多语言 MiniLM，支持中文，384 维，CPU 快速推理
sentence-transformers==3.0.1
# Qdrant：轻量备选
qdrant-client==1.9.1

# ── MinIO 对象存储 ───────────────────────────────────────────
minio==7.2.7

# ── 数据验证 ─────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ── 工具库 ───────────────────────────────────────────────────
python-dotenv==1.0.1
loguru==0.7.2
tenacity==8.3.0

# ── 监控（可选）─────────────────────────────────────────────
prometheus-fastapi-instrumentator==6.1.0
